<h1>位置情報を検索してみよう</h1>
<%  @client_spots.each do |spot|  %>
   <p><%= spot.name %></p>
<%end %>

<% @client.spots(-33.8670522, 151.1957362)%>
<%@client.spots(-33.8670522, 151.1957362, :types => 'restaurant')%>
<p><%= @client.spots(34.7055051, 135.4983028, :language => 'ja',:types => 'cafe')[0].inspect %></p>
<%= image_tag(@spot2.photos[0].fetch_url(800)) %>
<%= image_tag(@spot2.photos[1].fetch_url(800)) %>
<%= image_tag(@spot2.photos[2].fetch_url(800)) %>

<h1>希望の場所は見つかったかな?</h1>

<div class="col-md-12">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>Latitude</th>
        <th>Longitude</th>
        <th>Address</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @places.each do |place| %>
        <tr>
          <td><%= place.name %></td>
          <td><%= place.lat %></td>
          <td><%= place.lng %></td>
          <td><%= place.formatted_address %></td>
          <td><%= link_to( '登録', place_index_path( :place => { :name      => place.name,
                                                                 :latitude  => place.lat,
                                                                 :longitude => place.lng,
                                                                 :price_level => place.price_level,
                                                                 :rating => place.rating,
                                                                 :address   => place.formatted_address, } ),:method => 'post' ) %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <button type="button" class="btn pull-right btn-lg btn-default">
    <%= link_to 'Back', place_index_path %>
  </button>

</div>
